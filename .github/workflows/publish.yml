name: Release

on:
  push:
    branches: [main]

jobs:
  release:
    uses: wopr-network/.github/.github/workflows/release.yml@main
    permissions:
      contents: write
      issues: write
      pull-requests: write
      id-token: write
    secrets:
      NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
